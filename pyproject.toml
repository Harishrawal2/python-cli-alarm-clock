[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "alarm-clock-cli"
version = "0.1.0"
description = "A Python CLI alarm clock"
requires-python = ">=3.10"

[project.scripts]
alarm-clock = "alarm_clock.cli:run"
